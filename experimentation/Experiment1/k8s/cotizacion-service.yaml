apiVersion: v1
kind: Service
metadata:
  name: cotizacion
  namespace: solventa
  labels:
    app: cotizacion
    experimento: "1"
spec:
  type: ClusterIP
  selector:
    app: cotizacion
  ports:
    - name: http
      port: 8000
      targetPort: http
